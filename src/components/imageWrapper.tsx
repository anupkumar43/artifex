"use client";

import Editor from "~/components/editor";
import Recent from "~/components/recent";

const ImageWrapper = () => {
  return (
    <Editor>
      <Recent />
    </Editor>
  );
};

export default ImageWrapper;
